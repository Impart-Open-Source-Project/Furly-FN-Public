import 'package:flutter/material.dart';

class WatchLinkView extends StatelessWidget {
  const WatchLinkView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Watch Link placeHolder',
        style: TextStyle(
          color: Colors.white,
        ),
      ),
    );
  }
}
